{
  "name": "local-ai-os-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Frontier-tier relay: holds the Claude API key and the token ledger. Never runs on a student machine.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check:concurrency": "node scripts/concurrency-check.js",
    "check:spend": "node scripts/spend-down-check.js",
    "check:conversation": "node scripts/conversation-check.js",
    "calibrate": "node scripts/calibrate.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
